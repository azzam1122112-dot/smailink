# 🎯 تحديث تلقائي مراحل التسليم

## 📋 الملخص
تم تحديث نموذج الاتفاقيات لإدارة **مراحل التسليم بشكل تلقائي وديناميكي**:
- ✅ **إضافة تلقائية**: أضف مرحلة جديدة بضغطة زر واحدة
- ✅ **حذف فوري**: احذف مرحلة بضغطة زر "حذف" 
- ✅ **ترتيب ذكي**: يُحدّث الترتيب تلقائياً (1، 2، 3، ...)
- ✅ **واجهة بسيطة**: بدون حقول ترتيب يدوية معقدة
- ✅ **لا تحديث يدوي**: الترتيب يعود لحسابه الصحيح عند الحذف

## 🔄 التغييرات الرئيسية

### 1. **الواجهة الأمامية (HTML)**
```
┌─────────────────────────────────────────┐
│ مراحل تسليم المشروع  [+ إضافة مرحلة]  │
├─────────────────────────────────────────┤
│ ① عنوان المرحلة | المدة (أيام)        │
│    [التحليل]    | [5]                  │
│    [حذف]                               │
├─────────────────────────────────────────┤
│ ② عنوان المرحلة | المدة (أيام)        │
│    [التطوير]    | [10]                 │
│    [حذف]                               │
└─────────────────────────────────────────┘
```

### 2. **الميزات**
| الميزة | الوصف | التفصيل |
|--------|-------|---------|
| 🔢 **ترتيب تلقائي** | يبدأ من 1 | يُعدّل عند الحذف/الإضافة |
| ➕ **إضافة سريعة** | زر واحد | ينسخ قالب جديد تلقائياً |
| 🗑️ **حذف فوري** | بدون تأكيد | يُخفي الصف ويعدّل الترتيب |
| 🎨 **دائرة رقمية** | شريط جانبي | تعرض رقم الترتيب الحالي |
| 🔒 **حقل مخفي** | `order` | يُحدّث تلقائياً |

### 3. **الأعمدة (Columns)**
#### السابق:
```
[ عنوان الدفعة ] [ المبلغ ] [ الترتيب ] [ المهلة ]
```

#### الآن:
```
[①] [ عنوان المرحلة ] [ المدة (أيام) ] [حذف]
```

## 💻 كود JavaScript

### دالة `updateMilestoneOrders()`
تُحدّث الترتيب التلقائي:
```javascript
function updateMilestoneOrders() {
  const milestones = rows.querySelectorAll(".milestone-row:not([style*='display: none'])");
  milestones.forEach((el, idx) => {
    const orderInput = el.querySelector(".milestone-order-value");
    const orderDisplay = el.querySelector(".milestone-order");
    const newOrder = idx + 1;  // 1, 2, 3, ...
    if (orderInput) orderInput.value = newOrder;
    if (orderDisplay) orderDisplay.textContent = newOrder;
  });
}
```

### دالة `setupDeleteButtons()`
تُعد أزرار الحذف:
```javascript
function setupDeleteButtons() {
  rows.querySelectorAll(".delete-row").forEach(btn => {
    btn.addEventListener("click", function (e) {
      e.preventDefault();
      const row = this.closest(".milestone-row");
      const deleteCheckbox = row.querySelector("input[type='checkbox'][name*='-DELETE']");
      if (deleteCheckbox) {
        deleteCheckbox.checked = true;  // تفعيل الحذف
        row.style.display = "none";      // إخفاء الصف
        updateMilestoneOrders();          // إعادة ترتيب
      }
    });
  });
}
```

### زر الإضافة
```javascript
addBtn?.addEventListener("click", function (e) {
  e.preventDefault();
  const tmpl = document.getElementById("empty-form").innerHTML;
  const idx = parseInt(totalEl.value, 10);
  const html = tmpl.replaceAll("__prefix__", String(idx));
  
  const wrapper = document.createElement("div");
  wrapper.innerHTML = html;
  const newRow = wrapper.firstElementChild;
  rows.appendChild(newRow);
  
  totalEl.value = String(idx + 1);
  setupDeleteButtons();          // إعداد حذف للصف الجديد
  updateMilestoneOrders();        // إعادة ترتيب
});
```

## 🎮 التفاعل مع المستخدم

### السيناريو 1: إضافة مرحلة
```
1. المستخدم ينقر على "+ إضافة مرحلة"
   ↓
2. يظهر صف جديد بـ:
   - رقم ترتيب تلقائي (④ مثلاً)
   - حقول فارغة للعنوان والمدة
   - زر حذف
   ↓
3. المستخدم يملأ البيانات
   ↓
4. الترتيب يظل محفوظاً تلقائياً
```

### السيناريو 2: حذف مرحلة
```
1. المستخدم ينقر على "حذف" في المرحلة ②
   ↓
2. تُخفى المرحلة ② فوراً
   ↓
3. يُعاد الترتيب:
   - ① بقي ①
   - ③ أصبحت ②
   - ④ أصبحت ③
   ↓
4. حقل DELETE يُفعّل تلقائياً (خلف الكواليس)
```

### السيناريو 3: تعديل ثم حفظ
```
1. المستخدم يعدّل المراحل
2. ينقر على "حفظ وإرسال للعميل"
   ↓
3. يُرسل النموذج مع:
   - جميع الترتيبات المحدثة
   - علامات الحذف للصفوف المحذوفة
   - البيانات الصحيحة للمراحل المتبقية
```

## 🔍 التفاصيل التقنية

### الفئات CSS المستخدمة
```html
<!-- دائرة الترتيب -->
<div class="milestone-order">②</div>

<!-- صف المرحلة -->
<div class="milestone-row" data-row-index="1"></div>

<!-- حقل الترتيب المخفي -->
<input class="milestone-order-value" value="2">

<!-- زر الحذف -->
<button class="delete-row">حذف</button>
```

### العمليات التلقائية
```javascript
// عند تحميل الصفحة
✓ قراءة جميع المراحل الموجودة
✓ إعداد أزرار الحذف
✓ تحديث أرقام الترتيب

// عند الإضافة
✓ نسخ القالب الفارغ
✓ تحديث معرّف Formset
✓ تفعيل أزرار الحذف الجديدة
✓ إعادة حساب الترتيب

// عند الحذف
✓ وضع علامة DELETE
✓ إخفاء الصف من الواجهة
✓ إعادة حساب الترتيب والأرقام

// عند الإرسال
✓ إرسال جميع البيانات الصحيحة
✓ الصفوف المحذوفة تُحفظ مع DELETE=True
✓ الترتيب يبقى دقيقاً
```

## ⚙️ الإعدادات والحدود

### الحد الأقصى للمراحل
```python
# في forms.py - MilestoneFormSet
max_num = 50  # يمكن تعديله
```

### القيم الافتراضية
```python
# في forms.py - MilestoneForm
- title: بدون قيمة افتراضية
- due_days: بدون قيمة افتراضية
- order: يُحسب تلقائياً من قبل JS
```

## 🚀 الاستخدام

### للموظف (الإضافة):
```
1. افتح نموذج الاتفاقية
2. انقر على "+ إضافة مرحلة"
3. أدخل عنوان (مثال: تحليل المتطلبات)
4. أدخل المدة (مثال: 3 أيام)
5. كرر حسب الحاجة
6. انقر "حفظ وإرسال"
```

### للموظف (الحذف):
```
1. افتح نموذج الاتفاقية
2. جد المرحلة المراد حذفها
3. انقر على زر "حذف" الأحمر
4. ستُحذف المرحلة فوراً
5. الترتيب يُعدّل تلقائياً
6. انقر "حفظ"
```

## ✅ اختبارات موصى بها

```javascript
// اختبر الإضافة
✓ أضف مرحلة واحدة → الترتيب = 1
✓ أضف مرحلة ثانية → الترتيب = 1,2
✓ أضف ثالثة → الترتيب = 1,2,3

// اختبر الحذف
✓ احذف المرحلة الثانية → الترتيب = 1,2
✓ احذف الأولى → الترتيب = 1
✓ أضف جديدة → الترتيب = 1,2

// اختبر الحفظ
✓ أضف 3 مراحل، احذف الثانية، احفظ
✓ تحقق من DB أن الحذف سُجّل
✓ تحقق من الترتيب الصحيح
```

## 🐛 معالجة الأخطاء

إذا واجهت مشاكل:

### المشكلة: الترتيب لا يتحدث
**الحل**: تأكد من أن `updateMilestoneOrders()` تُستدعى بعد كل عملية

### المشكلة: الحذف لا يعمل
**الحل**: تحقق من أن `setupDeleteButtons()` مستدعاة بعد الإضافة

### المشكلة: الترتيب يعود للخلف
**الحل**: تأكد من أن حقل `milestone-order-value` موجود وله `name` صحيح

## 📞 الدعم
إذا احتجت تعديلات إضافية أخبرني بالتفاصيل!
